const _ = require('lodash');

/*
    Any modifications that need to be made to the types done here.
 */
module.exports = {
    /*
        Takes the list of types as a parameter.
        Creates each string type, into an object with key to be used by selectize.
        Returns array of objects.
     */
    creatTypeObj: function (type_list) {
        console.dir(type_list); // debug - remove
    }
}
